The program reads in the input.txt which in each row contains the number of units purchased by a client and 
from the table.txt loads the table of prices to interpolate.
Those files must be in the same directory as the SalesCalculator.jar file.